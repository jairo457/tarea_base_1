import 'package:flutter/widgets.dart';

class GlobalValues {
  static ValueNotifier<bool> flagTheme = ValueNotifier<bool>(true);
  static ValueNotifier<bool> flagCareer = ValueNotifier<bool>(true);
  static ValueNotifier<bool> flagProfesor = ValueNotifier<bool>(true);
  static ValueNotifier<bool> flagTask = ValueNotifier<bool>(true);
  //Valor que cmabiara para actualizar la vita de las tareas
}
